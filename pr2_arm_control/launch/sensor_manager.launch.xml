<launch>

  <!-- This file makes it easy to include the settings for sensor managers -->  

  <!-- Set the file describing which sensor(s) to use to build the octomap  -->
  <arg name="moveit_octomap_sensor_params_file" default="$(find pr2_arm_control)/config/sensors_kinect.yaml" />

  <arg name="octomap_resolution" default="0.015" />

  <!-- Load the robot specific sensor manager; this sets the moveit_sensor_manager ROS parameter -->
  <arg name="moveit_sensor_manager" default="pr2" />
  <include file="$(find pr2_arm_control)/launch/$(arg moveit_sensor_manager)_moveit_sensor_manager.launch.xml">
    <arg name="moveit_octomap_sensor_params_file" value="$(arg moveit_octomap_sensor_params_file)"/>
    <arg name="octomap_resolution" value="$(arg octomap_resolution)" />
  </include>

  
</launch>
